name: weather_app
description: Weather app optimized for Realme C35 (Android 11, FHD+ 1080x2408).
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  google_maps_flutter: ^2.6.0
  flutter:
    sdk: flutter
  http: ^1.2.0
  shared_preferences: ^2.5.5
  geolocator: ^14.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true